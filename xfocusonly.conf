# xfocusonly: privileged executables, one absolute path per line.
# Privileged clients keep full X11 semantics (raw key events, RECORD, root
# hotkeys, clipboard). Your window manager belongs here. Nothing else should,
# unless you know exactly why. See the header of xfocusonly.c.

/usr/local/bin/dwm
